import React from 'react'
import styles from './navbar.module.css'

const Navbar = () => (
    <nav
        className={styles.container}
    >
        From the nav component
    </nav>
)

export default Navbar